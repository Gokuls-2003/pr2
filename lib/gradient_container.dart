import 'package:flutter/material.dart';

class GradientContaioner extends StatelessWidget{
  const GradientContaioner({super.key});

  @override
  Widget build(BuildContext context){
        return Container(
          decoration: BoxDecoration(
            
          ),
        );
  }
}